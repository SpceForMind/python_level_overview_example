## Execute:
```bash
python -m uploader -h
usage: __main__.py [-h] {docx_uploader} ...

File Uploaders

positional arguments:
  {docx_uploader}
    docx_uploader  Upload file [docx]

optional arguments:
  -h, --help       show this help message and exit

python -m uploader docx_uploader -h
usage: __main__.py docx_uploader [-h] --file FILE

optional arguments:
  -h, --help   show this help message and exit
  --file FILE  path to docx file
```
